# Optimize npm for Vercel deployment
# Only install production dependencies
production=true
# Skip optional dependencies
optional=false
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Reduce bundle size by not including dev dependencies in production
save-prod=false
save-dev=false

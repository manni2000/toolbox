# Disable npm package integrity checks to handle corrupted packages
# This is needed for Vercel deployment when packages are corrupted
strict-ssl=false
# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true
# Skip integrity checks for problematic packages
# Note: This is a temporary workaround for deployment issues
integrity=false
